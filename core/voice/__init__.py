"""Voice modules for JARVIS (wake word, STT, TTS).

These started as standalone experiment scripts; they are consolidated into the
voice pipeline that the brain/app use. See `core/app/main.py` voice endpoints.
"""
